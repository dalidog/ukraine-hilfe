Options +FollowSymLinks
RewriteEngine On
RewriteBase /

# 1. If file or folder exists, serve it
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# 2. Otherwise send everything to React
RewriteRule ^ index.html [L]